# Copy to .env (do NOT commit .env)
# Then: source .env  (or use a direnv tool)

OPENAI_API_KEY="paste-your-openai-key-here"

# Optional: if you want the agent to auto-load your config
XAIO_CONFIG_PATH="./config.yaml"

# Optional: Brave CDP port (if you change it)
BRAVE_CDP_PORT="9222"
