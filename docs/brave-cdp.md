# Brave + CDP (Chrome DevTools Protocol)

Some sites require a real browser session. This project uses Brave running locally with CDP enabled.

## Launch Brave with remote debugging

Example:
```bash
brave-browser-beta --remote-debugging-port=9222 --user-data-dir="$HOME/.config/BraveSoftware/Brave-Browser-Beta-Profile-xaio"
